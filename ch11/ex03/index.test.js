import { littleToBig, bigToLittle } from "./index.js";

describe("isEmailAddress", () => {
  it("little to big", () => {
    expect(littleToBig()).toBe();
    expect(littleToBig()).toBe();
    expect(littleToBig()).toBe();
    expect(littleToBig()).toBe();
  });

  it("big to little", () => {
    expect(bigToLittle()).toBe();
    expect(bigToLittle()).toBe();
    expect(bigToLittle()).toBe();
    expect(bigToLittle()).toBe();
  });
});
