//↓の文字をエスケープする
//「0」「b」「t」「n」「v」「f」「r」「"」「'」

function escape1(arg) {
  //if elseで分岐する
  if (arg.includes("0")) {
  }
  if (arg.includes("b")) {
  }
  if (arg.includes("t")) {
  }
  if (arg.includes("n")) {
  }
  if (arg.includes("v")) {
  }
  if (arg.includes("f")) {
  }
  if (arg.includes("r")) {
  }
  if (arg.includes('"')) {
  }
  if (arg.includes("'")) {
  }
}

function escape2(arg) {
  //switchで分岐する
}

escape1("ex0");
escape2("ex0");
