//↓の文字をエスケープする
//「0」「b」「t」「n」「v」「f」「r」「"」「'」
//例えば「ex\01」は「ex\\01」として出力する

function escape1(arg) {
  //if elseで分岐する
}

function escape2(arg) {
  //switchで分岐する
}

escape1("ex\\01");
escape2("ex\\01");
